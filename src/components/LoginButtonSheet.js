import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LoginButtonSheet = () => {
  return (
    <View>
      <Text>LoginButtonSheet</Text>
    </View>
  );
};

export default LoginButtonSheet;

const styles = StyleSheet.create({});
